import React from "react";
import { AddContract } from "../components";

const CreateContract = () => {
  return (
    <div className="container">
      <AddContract />
    </div>
  );
};

export default CreateContract;
